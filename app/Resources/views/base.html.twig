<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('bundles/ivan1986deb/bootstrap/css/bootstrap.min.css') }}" />
        <style>
            body {
                margin: 40px 0;
            }
        </style>
        {% block stylesheets %}
        {% endblock %}
        <link rel="stylesheet" href="{{ asset('bundles/ivan1986deb/bootstrap/css/bootstrap-responsive.min.css') }}" />
        {% stylesheets filter="compass" "@Ivan1986DebBundle/Resources/assets/css/*.scss" %}
        <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}
        <script type="text/javascript" src="{{ asset('bundles/ivan1986deb/bootstrap/js/bootstrap.min.js') }}"></script>
        {#
        {% javascripts '@Ivan1986DebBundle/Resources/assets/js/*' %}
        <script type="text/javascript" src="{{ asset_url }}"></script>
        {% endjavascripts %}
        #}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div class="navbar navbar-fixed-top">
            <div class="navbar-inner">
                <div class="container">
                    <a class="brand" href="{{ url('home') }}">PkgGen</a>
                    <ul class="nav">
                        {% block top_nav %}{% endblock %}
                    </ul>
                    <div class="navbar-text pull-right">
                        {% block login %}{% endblock %}
                    </div>
                </div>
            </div>
        </div>
        <div class="container">
        {% block body %}{% endblock %}
        </div>
        <div class="navbar navbar-fixed-bottom">
            <div class="navbar-inner">
                <div class="container">
                    <div class="navbar-text pull-left">
                        <a href="https://github.com/ivan1986/deb-pkg-gen">исходный код на GitHub</a>
                    </div>
                    <div class="navbar-text pull-right">Сопровождающий: {{maintainer}}</div>
                </div>
            </div>
        </div>
        {% block javascripts %}
        {% endblock %}
    </body>
</html>
