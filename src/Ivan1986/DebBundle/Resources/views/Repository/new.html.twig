<h1>Repository creation</h1>

<form action="{{ path('repos_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('repos') }}">
            Back to the list
        </a>
    </li>
</ul>
